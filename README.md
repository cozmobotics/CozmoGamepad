# CozmoGamepad
small demo program to use a gamepad for steering Cozmo

You need to install the "inputs" library, like
pip[3] install inputs

And you need, of course a Cozmo robot with SDK installed 
and a gamepad. 
Any gamepad shousd do, I tested it only with a speedlink strike NX


